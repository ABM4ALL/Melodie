# covid_contagion_mesa

This is the Mesa comparison example corresponding to the Melodie COVID contagion model. It is intended for comparing modeling structure and outputs across frameworks.

Dependencies:

- `mesa`

Run from the repository root:

```bash
python -m examples.covid_contagion_mesa.main
```

Outputs are written to `examples/covid_contagion_mesa/data/output/`.
