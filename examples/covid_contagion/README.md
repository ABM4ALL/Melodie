# covid_contagion

This is the minimal COVID contagion example for Melodie. It demonstrates the standard interaction between `Scenario`, `Model`, `Environment`, `DataCollector`, and `Simulator`.

Run from the repository root:

```bash
python -m examples.covid_contagion.main
```

Outputs are written to `examples/covid_contagion/data/output/`.
