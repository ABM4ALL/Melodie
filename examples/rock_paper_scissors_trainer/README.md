# rock_paper_scissors_trainer

This example demonstrates how to use `Trainer` to optimize strategy parameters in a rock-paper-scissors model with a genetic algorithm.

Run from the repository root:

```bash
python -m examples.rock_paper_scissors_trainer.main
```

Outputs are written to `examples/rock_paper_scissors_trainer/data/output/`.
