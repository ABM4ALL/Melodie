# covid_contagion_grid

This example extends the base COVID contagion model with `Grid`, showing how to represent spatial locations and neighborhood-based contagion.

Run from the repository root:

```bash
python -m examples.covid_contagion_grid.main
```

Outputs are written to `examples/covid_contagion_grid/data/output/`.
