# covid_contagion_network

This example extends the base COVID contagion model with `Network`, showing how contagion can be modeled on top of a network topology.

Run from the repository root:

```bash
python -m examples.covid_contagion_network.main
```

Outputs are written to `examples/covid_contagion_network/data/output/`.
